version: "2"
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: "query.sql"
    gen:
      go:
        package: "db"
        out: "."
        sql_package: "database/sql"
        emit_interface: true
        emit_json_tags: true
